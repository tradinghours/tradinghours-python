
# CSVReader?


# Writer

class Writer:
    """
    Reads csv files and writes data to database

    Needs to read csv files in a certain order and
    """

    def __init__(self, remote: StrOrPath):
      takes the path to remote/


    # def extract(file_path)
    #   iterates over rows in file_path
    #   if there are no rows, return False
    #   otherwise save to matching table


    # def extract_all
    #   iterate over file_paths of remote folder
    #   call self.extract on each of them
    #   if self.extract





